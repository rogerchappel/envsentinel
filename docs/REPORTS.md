# Reports

Markdown reports are meant for humans and pull request comments. JSON reports are stable enough for automation.

Every issue includes:

- issue code
- variable name
- severity
- evidence locations
- remediation guidance

The `generatedAt` field is pinned to the Unix epoch for deterministic local diffs.
